# Browser fetches use relative /api/* URLs (the Netlify rewrite forwards them to
# api.swimbuddz.com; in dev, next.config rewrites to localhost:8000). Only set
# this if a Server Component fetches the API with an absolute URL.
# NEXT_PUBLIC_API_BASE_URL=https://api.swimbuddz.com
